export interface Article {
    id: string;
    title: string;
    published_at: string;
    image_url: string;
    summary: string;
    url: string;
  }
  
  export interface ArticleDetailProps {
    id: string;
  }
  